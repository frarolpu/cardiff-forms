services:
  - type: web
    name: cardiff-forms
    runtime: python
    startCommand: gunicorn --workers 2 --preload --bind 0.0.0.0:$PORT --timeout 120 app:app
    buildCommand: pip install -r requirements.txt
    env:
      - key: PYTHON_VERSION
        value: 3.11
